name: pulumi-avd
description: A minimal Python Pulumi program
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
    main: __main_cv__.py
config:
  pulumi:tags:
    value:
      pulumi:template: python
